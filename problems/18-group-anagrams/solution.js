/**
 * Given an array of strings strs, group the anagrams together. An anagram
 * is a word formed by rearranging the letters of another word using all
 * the original letters exactly once. Return the groups in any order, and
 * the elements within each group in any order.
 *
 * @param {string[]} strs
 * @return {string[][]}
 */
function groupAnagrams(strs) {
  // TODO: implement
}

module.exports = { groupAnagrams };
