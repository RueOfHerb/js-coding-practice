/**
 * Given two strings s and t, return the minimum-length substring of s that
 * contains every character of t, including duplicates (if t has two 'A's,
 * the returned substring must contain at least two 'A's). If no such
 * substring exists, return an empty string "".
 *
 * @param {string} s
 * @param {string} t
 * @return {string}
 */
function minWindow(s, t) {
  // TODO: implement
}

module.exports = { minWindow };
