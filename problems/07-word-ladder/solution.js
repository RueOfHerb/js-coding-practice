/**
 * Given two words, beginWord and endWord, and a dictionary wordList, return
 * the number of words in the shortest transformation sequence from
 * beginWord to endWord, such that:
 * - Only one letter can be changed at a time.
 * - Each transformed word must exist in wordList. Note that beginWord does
 *   not need to be in wordList.
 * If there is no such transformation sequence, return 0.
 * All words have the same length and consist of lowercase English letters.
 *
 * @param {string} beginWord
 * @param {string} endWord
 * @param {string[]} wordList
 * @return {number}
 */
function ladderLength(beginWord, endWord, wordList) {
  // TODO: implement
}

module.exports = { ladderLength };
