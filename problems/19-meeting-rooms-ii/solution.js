/**
 * Given an array of meeting time intervals `intervals` where
 * intervals[i] = [starti, endi], return the minimum number of conference
 * rooms required so that no two meetings assigned to the same room overlap.
 * A meeting that ends exactly when another begins is not considered an
 * overlap, so those two meetings may share a room.
 *
 * @param {number[][]} intervals
 * @return {number}
 */
function minMeetingRooms(intervals) {
  // TODO: implement
}

module.exports = { minMeetingRooms };
