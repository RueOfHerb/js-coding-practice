/**
 * There are numCourses courses labeled 0 to numCourses - 1. You are given an
 * array prerequisites where prerequisites[i] = [a, b] indicates you must
 * take course b before course a. Return true if you can finish all courses.
 *
 * @param {number} numCourses
 * @param {number[][]} prerequisites
 * @return {boolean}
 */
function canFinish(numCourses, prerequisites) {
  // TODO: implement
}

module.exports = { canFinish };
